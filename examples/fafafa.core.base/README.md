# Examples for fafafa.core.base

本目录收纳 `fafafa.core.base` 的最小可运行示例。

## Current entry

- Linux/macOS：`examples/fafafa.core.base/BuildOrRun.sh`
- Windows：`examples\fafafa.core.base\BuildOrRun.bat`
- 示例源码：`examples/fafafa.core.base/example_base.lpr`
- Lazarus 工程：`examples/fafafa.core.base/example_base.lpi`

## What to run first

- `example_base.lpr`
  - 适合作为 `fafafa.core.base` 的最小 smoke example
  - 如果只想确认示例入口是否仍然可运行，优先从这个文件开始

## Generated outputs

- `bin/` 和 `lib/` 只代表本地构建结果，不是 source-of-truth。
- current-entry 应回到 README、`BuildOrRun*` 脚本、`.lpr` / `.lpi` 源文件，而不是把生成产物当成稳定入口。
