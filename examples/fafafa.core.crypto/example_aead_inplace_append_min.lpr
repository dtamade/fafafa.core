{$CODEPAGE UTF8}
program example_aead_inplace_append_min;

{$mode objfpc}{$H+}

uses
  SysUtils,
  fafafa.core.crypto,
  fafafa.core.crypto.interfaces;

begin
  // 请直接运行 example_aead_inplace_append_min.pas（同名单文件程序）
  // 该占位仅用于 IDE 可见性；核心逻辑在 .pas 中
end.

