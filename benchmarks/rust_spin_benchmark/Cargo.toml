[package]
name = "rust_spin_benchmark"
version = "0.1.0"
edition = "2021"

[dependencies]
parking_lot = "0.12"
spin = "0.9"
crossbeam = "0.8"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
