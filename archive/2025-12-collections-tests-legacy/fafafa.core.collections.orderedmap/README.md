# tests_orderedmap

- 运行（从仓库根目录）：
  - `tests\fafafa.core.collections.orderedmap\BuildOrTest.bat`
- 运行（从本目录）：
  - `lazbuild --build-mode=Debug tests_orderedmap.lpi && ..\..\bin\tests_orderedmap.exe --all --format=plain`

